<div class="error-container">
    <div class="error-content">
        <div class="error-code">404</div>
        <h1 class="error-title">页面未找到</h1>
        <p class="error-description">
            抱歉，您访问的页面似乎不存在或已被移除。
        </p>

        <div class="error-suggestions">
            <h3>您可以尝试：</h3>
            <ul>
                <li>
                    <a href="<%= config.root %>" class="suggestion-link">
                        <i class="fas fa-home"></i> 返回首页
                    </a>
                </li>
                <li>
                    <a href="<%= config.root %>categories/" class="suggestion-link">
                        <i class="fas fa-folder"></i> 浏览分类
                    </a>
                </li>
                <li>
                    <a href="<%= config.root %>archives/" class="suggestion-link">
                        <i class="fas fa-archive"></i> 查看归档
                    </a>
                </li>
                <li>
                    <a href="<%= config.root %>tags/" class="suggestion-link">
                        <i class="fas fa-tags"></i> 浏览标签
                    </a>
                </li>
            </ul>
        </div>

        <div class="error-search">
            <p>或者使用搜索功能：</p>
            <div class="search-box">
                <input type="text" id="search-input" class="search-input" placeholder="输入关键词...">
                <button id="search-btn" class="search-btn">
                    <i class="fas fa-search"></i> 搜索
                </button>
            </div>
        </div>
    </div>
</div>

<style>
.error-container {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 60vh;
    padding: 2rem 1rem;
}

.error-content {
    text-align: center;
    max-width: 600px;
}

.error-code {
    font-size: 8rem;
    font-weight: bold;
    color: var(--primary-color, #2563eb);
    opacity: 0.1;
    margin-bottom: -2rem;
}

.error-title {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: var(--text-color, #333);
}

.error-description {
    font-size: 1.1rem;
    color: var(--text-secondary, #666);
    margin-bottom: 2rem;
}

.error-suggestions {
    margin-bottom: 2rem;
}

.error-suggestions h3 {
    margin-bottom: 1rem;
    color: var(--text-color, #333);
}

.error-suggestions ul {
    list-style: none;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 1rem;
}

.suggestion-link {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 1rem;
    background: var(--bg-secondary, #f5f5f5);
    border-radius: 8px;
    text-decoration: none;
    color: var(--primary-color, #2563eb);
    transition: all 0.3s ease;
}

.suggestion-link:hover {
    background: var(--primary-color, #2563eb);
    color: white;
}

.error-search {
    margin-top: 2rem;
    padding-top: 2rem;
    border-top: 1px solid var(--border-color, #eee);
}

.search-box {
    display: flex;
    gap: 0.5rem;
    margin-top: 1rem;
}

.search-input {
    flex: 1;
    padding: 0.75rem;
    border: 1px solid var(--border-color, #ddd);
    border-radius: 4px;
    font-size: 1rem;
}

.search-btn {
    padding: 0.75rem 1.5rem;
    background: var(--primary-color, #2563eb);
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    transition: background 0.3s ease;
}

.search-btn:hover {
    background: var(--primary-color-dark, #1d4ed8);
}
</style>
