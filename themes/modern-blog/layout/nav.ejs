<nav class="navbar">
    <div class="nav-container">
        <div class="nav-logo">
            <a href="<%= config.root %>">
                <i class="fas fa-code"></i>
                <span><%= config.title %></span>
            </a>
        </div>

        <div class="nav-menu">
            <a href="<%= config.root %>" class="nav-item <% if (is_home()) { %>active<% } %>">
                <i class="fas fa-home"></i> 首页
            </a>
            <a href="<%= config.root %>categories/" class="nav-item <% if (is_category()) { %>active<% } %>">
                <i class="fas fa-folder"></i> 分类
            </a>
            <a href="<%= config.root %>writer/" class="nav-item <% if (page.title === '写作编辑器') { %>active<% } %>">
                <i class="fas fa-pen-fancy"></i> 写作
            </a>
            <a href="<%= config.root %>about/" class="nav-item <% if (page.title === 'About' || page.title === '关于') { %>active<% } %>">
                <i class="fas fa-user"></i> 关于
            </a>
        </div>

        <div class="nav-search">
            <div class="search-container">
                <input type="text" class="search-input" id="search-input" placeholder="搜索文章...">
                <button class="search-btn">
                    <i class="fas fa-search"></i>
                </button>
            </div>
            <div class="search-results" id="search-results" style="display: none;">
                <div class="search-results-list"></div>
            </div>
        </div>

        <button class="theme-toggle" id="theme-toggle" title="切换主题">
            <i class="fas fa-moon"></i>
        </button>

        <div class="nav-toggle">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </div>
</nav>
